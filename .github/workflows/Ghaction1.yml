name: first workflow

on: workflow_dispatch

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print greeting
        run: echo "HELLO WORLD!"
      - name: print good bye
        run: echo "DONE---GOOD-BYE!"
